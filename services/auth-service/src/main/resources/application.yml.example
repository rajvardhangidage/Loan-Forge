# ===================================================================
# Auth Service Configuration Template
# Copy this file to 'application.yml' in the same directory:
#   cp application.yml.example application.yml
# ===================================================================

server:
  port: 8081

spring:
  application:
    name: auth-service
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/authdb?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true}
    username: ${DB_USERNAME:your_db_username}
    password: ${DB_PASSWORD:your_db_password}
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        type:
          preferred_uuid_jdbc_type: VARCHAR
    open-in-view: false
  flyway:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,info

app:
  jwt:
    # Use a secure, random 256-bit (at least 32 characters) secret key in production
    secret: ${JWT_SECRET:replace-with-a-secure-256-bit-secret-key-for-jwt-tokens}
    expiration-minutes: 30
