# ===================================================================
# Customer Service Configuration Template
# Copy this file to 'application.yml' in the same directory:
#   cp application.yml.example application.yml
# ===================================================================

server:
  port: 8082

spring:
  application:
    name: customer-service
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/customerdb}
    username: ${DB_USERNAME:your_db_username}
    password: ${DB_PASSWORD:your_db_password}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,info

app:
  jwt:
    # Must match the JWT secret configured across your services
    secret: ${JWT_SECRET:replace-with-a-secure-256-bit-secret-key-for-jwt-tokens}
